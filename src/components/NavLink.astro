---
export interface Props {
  name: string;
  link: string;
  color: string;
}

const { name, link, color } = Astro.props;
---

<a
  href={link}
  class="text-sm font-medium uppercase tracking-wid
         hover:opacity-80 transition"
  style={{ color: `var(${color})` }}
>
  {name}
</a>
